server:
  port: 8888

gitConfigServer: github.com

spring:
  application:
    name: configuration-service
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://${gitConfigServer}/mehdikhelif/config-server-config.git